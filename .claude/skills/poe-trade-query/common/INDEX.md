# Shared (realm-agnostic) knowledge index

Everything indexed here applies to **both POE1 and POE2** — it's about the website and about method, not about either game. Safe to read on any realm. Game-specific facts live in `../poe1/knowledge/` and `../poe2/knowledge/`; grep the current realm's INDEX too.

Grep with terms from the user's request — include both 中文 and English synonyms. Read only the matching file(s).

- [tricks.md](tricks.md) — Cloudflare 403 blocked 被擋 User-Agent UA fallback 備援 escalation ladder cookie cache 暫存 .poe_cookies.json 過期 被刪除 disposable 拋棄式 saved_at 檢查 cf_clearance POESESSID POETOKEN sec-ch-ua headers 擬真 refresh-on-trigger 更新暫存 回寫 re-export 觸發條件 只要開瀏覽器 permission blocked 被權限擋 靜默失敗 silently 別假設成功 permissions.allow rate limit 限流 429 Retry-After 懲罰 X-Rate-Limit policy state hidden quota 隱藏配額 header decoy 假象 login pool 登入分池 authenticated anonymous 匿名 Account dimension 維度 anti-rapid-click 連點防護 VPN switch 切換 無效 debunked cross-game budget 跨遊戲共用配額 deep fetch 深抓 高價端 price ladder probe ceiling 先探頂 撈池 sampling bias 取樣偏差 multi-slot combo 多部位配裝 最佳化 pool per slot 本地暴力 bucket cap 桶上限 convergence 收斂 marginal value 邊際效益 script 腳本 gear_combo_optimizer.py no goto 不開網頁 query_id URL 交付連結 price cap equivalent mode 等值模式 internal rate 內部匯率 exchange board unreliable 交易板不可信 POESESSID expired 過期 先問使用者 ask first 手動更新 60 秒 timeout fallback 順序 UA 紀律 不要寫死 never hardcode user_agent 來源 source of truth 註銷 revoked invalidated session Chrome 版本 漂移 drift saved_at 自然過期 aged out 診斷 in-page fetch 頁內 already logged-in 已登入瀏覽器 persistent profile playwright-cli run-code --filename argv cmd.exe percent 百分號 吃掉 SyntaxError WinError 2 npm shim .cmd clamp 截頂 surplus 溢出 suffix-max 後綴最大值 3-D lattice Pareto frontier 前緣 exact 精確解 prune_groups lossy 有損 bucket cap 桶截斷 次佳解 brute force 對拍
- [optimizer-algorithms.md](optimizer-algorithms.md) — 求解演算法對照 compare algorithms 世代 generation 失真 lossy 失真點 精度 accuracy 耗時 runtime 慢 slow 旋鈕 knob 參數 cap 預設值 default 怎麼定 50000 POE_FAST 收斂 convergence 加倍測試 doubling test 桶截斷 bucket prune_groups 分階組合 staged combine 截頂 clamp Pareto 前緣 後綴最大值 suffix-max 對拍 brute force selftest 只會低估 understate 中間級距 mid tier 頭條級距 headline 新增條目樣板 template 該用哪一套
- [delivery.md](delivery.md) — deliverable 交付 標準 format 檔案 search_results timestamp 時間戳 購買連結 markdown hyperlink 表格 table 部位 名稱 價格 備註 總價格 描述 需求檢核 驗證在架 seller-filtered 賣家過濾 甜蜜點 sweet spot
